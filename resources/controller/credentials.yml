---

- name: "{{ asset_prefix }} Peripheral Credentials"
  credential_type: "Peripheral Credentials"
  organization: "{{ controller_organization }}"
- name: "{{ asset_prefix }} Root Account"
  credential_type: Machine
  organization: "{{ controller_organization }}"
  inputs:
    username: root
    password: "{{ ansible_password }}"